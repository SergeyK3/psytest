# Настройка systemd сервиса для PsychTest Telegram Bot

## 🎯 Возможности systemd сервиса

✅ **Автозапуск при загрузке системы**  
✅ **Контроль единственного экземпляра (через PID)**  
✅ **Автоматический перезапуск при сбоях**  
✅ **Работа без SSH-сессии**  
✅ **Изоляция от пользовательской сессии**  
✅ **Ограничение ресурсов (ОЗУ, CPU)**  
✅ **Централизованное логирование**  

## 📋 Быстрая установка

### 1. Скопируйте файлы на сервер

```bash
# Скопируйте весь проект в домашнюю директорию на сервере
scp -r ./PsychTest user@your-server:~/MyApps/
```

### 2. Установите сервис

```bash
# Перейдите в директорию проекта
cd ~/MyApps/PsychTest

# Сделайте скрипт исполняемым
chmod +x manage-service.sh

# Установите зависимости и сервис
./manage-service.sh deps     # Установка Python зависимостей
./manage-service.sh install  # Установка systemd сервиса
```

### 3. Запустите сервис

```bash
# Запуск сервиса
./manage-service.sh start

# Проверка статуса
./manage-service.sh status

# Просмотр логов
./manage-service.sh logs
```

## 🔧 Команды управления

### Основные команды

```bash
./manage-service.sh install    # Установить сервис
./manage-service.sh start      # Запустить
./manage-service.sh stop       # Остановить
./manage-service.sh restart    # Перезапустить
./manage-service.sh status     # Статус
./manage-service.sh logs       # Логи
./manage-service.sh follow     # Логи в реальном времени
./manage-service.sh uninstall  # Удалить сервис
```

### Прямые systemctl команды

```bash
# Управление сервисом
sudo systemctl start psychtest-bot
sudo systemctl stop psychtest-bot
sudo systemctl restart psychtest-bot
sudo systemctl status psychtest-bot

# Автозапуск
sudo systemctl enable psychtest-bot   # Включить автозапуск
sudo systemctl disable psychtest-bot  # Отключить автозапуск

# Логи
journalctl -u psychtest-bot -f        # Логи в реальном времени
journalctl -u psychtest-bot -n 100    # Последние 100 строк
journalctl -u psychtest-bot --since "1 hour ago"  # За последний час
```

## 📊 Мониторинг

### Проверка статуса

```bash
# Краткий статус
systemctl is-active psychtest-bot     # active/inactive
systemctl is-enabled psychtest-bot    # enabled/disabled

# Подробный статус
systemctl status psychtest-bot --no-pager -l
```

### Анализ производительности

```bash
# Использование ресурсов
systemctl show psychtest-bot --property=MemoryCurrent,CPUUsageNSec,TasksCurrent

# Статистика перезапусков
systemctl show psychtest-bot --property=NRestarts,RestartUSec
```

## 🛡️ Безопасность и ограничения

Сервис настроен с ограничениями безопасности:

- **MemoryMax=1G** - максимум 1ГБ ОЗУ
- **CPUQuota=50%** - максимум 50% одного ядра CPU
- **TasksMax=100** - максимум 100 процессов/потоков
- **PrivateTmp=true** - изолированная временная директория
- **ProtectSystem=strict** - только чтение системных директорий
- **NoNewPrivileges=true** - запрет повышения привилегий

## 📁 Структура файлов

```
PsychTest/
├── telegram_test_bot.py        # Основной файл бота
├── psychtest-bot.service       # Конфигурация systemd
├── manage-service.sh           # Скрипт управления
├── requirements.txt            # Python зависимости
├── .env                       # Переменные окружения
└── SYSTEMD_SETUP.md           # Эта инструкция
```

## 🔧 Настройка переменных окружения

Создайте файл `.env` со следующими переменными:

```bash
# Обязательные переменные
BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here

# Опциональные переменные
GOOGLE_DRIVE_FOLDER_ID=your_google_drive_folder_id
LOG_LEVEL=INFO
```

## 🚨 Устранение неполадок

### Сервис не запускается

```bash
# Проверить детальный статус
systemctl status psychtest-bot -l

# Проверить логи
journalctl -u psychtest-bot --since "10 minutes ago"

# Проверить конфигурацию
sudo systemd-analyze verify /etc/systemd/system/psychtest-bot.service
```

### Бот не отвечает

```bash
# Перезапустить сервис
sudo systemctl restart psychtest-bot

# Проверить логи на ошибки
journalctl -u psychtest-bot -p err

# Проверить доступность Telegram API
curl -s "https://api.telegram.org/bot$BOT_TOKEN/getMe"
```

### Высокое потребление ресурсов

```bash
# Проверить использование ресурсов
systemctl show psychtest-bot --property=MemoryCurrent,CPUUsageNSec

# Проверить процессы
ps aux | grep telegram_test_bot

# Анализ производительности
top -p $(systemctl show psychtest-bot --property=MainPID --value)
```

## 📈 Обновление бота

```bash
# 1. Остановить сервис
./manage-service.sh stop

# 2. Обновить код
git pull origin main

# 3. Обновить зависимости (если нужно)
./manage-service.sh deps

# 4. Запустить сервис
./manage-service.sh start

# 5. Проверить статус
./manage-service.sh status
```

## ⚙️ Продвинутая настройка

### Изменение ограничений ресурсов

Отредактируйте файл `/etc/systemd/system/psychtest-bot.service`:

```ini
[Service]
MemoryMax=2G        # Увеличить до 2ГБ
CPUQuota=100%       # Полное ядро CPU
TasksMax=200        # Больше процессов
```

После изменений:

```bash
sudo systemctl daemon-reload
sudo systemctl restart psychtest-bot
```

### Настройка логирования

```bash
# Изменить уровень логирования
sudo systemctl edit psychtest-bot

# Добавить:
[Service]
Environment=LOG_LEVEL=DEBUG
```

### Уведомления о сбоях

Создайте скрипт уведомлений `/usr/local/bin/notify-bot-failure.sh`:

```bash
#!/bin/bash
# Отправка уведомления при сбое бота
echo "PsychTest Bot failed on $(hostname) at $(date)" | mail -s "Bot Failure Alert" admin@example.com
```

Добавьте в сервис:

```ini
[Service]
ExecStartPost=/usr/local/bin/notify-bot-failure.sh
```

## 🎯 Рекомендации по эксплуатации

1. **Регулярно проверяйте логи** на наличие ошибок
2. **Мониторьте использование ресурсов** 
3. **Делайте резервные копии** конфигурации и данных
4. **Обновляйте зависимости** регулярно
5. **Тестируйте** обновления в staging окружении

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте статус: `./manage-service.sh status`
2. Посмотрите логи: `./manage-service.sh logs`
3. Перезапустите: `./manage-service.sh restart`
4. Обратитесь к документации systemd: `man systemd.service`

---

🚀 **Готово!** Ваш бот теперь работает как системный сервис с автозапуском и автоматическим восстановлением.