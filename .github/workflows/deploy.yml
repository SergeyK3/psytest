name: Deployment disabled

# Deliberately no push trigger. Beget deployment is retired and PS.kz remains
# a manual operator procedure until separately approved.
on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic deployment is disabled; follow PS_KZ_DEPLOYMENT.md."
